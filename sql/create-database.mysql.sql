-- Run once as a MySQL administrator. Fails if this name is already in use;
-- inspect any existing database instead of overwriting it.
CREATE DATABASE canvas CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
